import { ExpanderToggleDirective } from './expander-toggle.directive';

describe('ExpanderToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpanderToggleDirective();
    expect(directive).toBeTruthy();
  });
});
